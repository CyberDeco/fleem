[package]
name = "fleem"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Design tokens and style recipes for floem 0.2 applications"

[dependencies]
floem = "0.2"
floem_reactive = "0.2"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
